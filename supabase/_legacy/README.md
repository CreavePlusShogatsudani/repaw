# _legacy

2026-09-04 以前のスキーマファイル。本番DBと乖離しており、実行してはいけない。
現在の正は `migrations/000_baseline.sql`（本番スナップショット）＋ `004` 以降。

- schema.sql / admin_schema.sql : 初期スキーマ（手作業で入れた変更が反映されていない）
- 001〜002 : 追加テーブル（本番の buyback_requests とカラムが異なる）
- 003 : セキュリティ修正。本番には未適用だったため、004 として冪等化して適用済み
